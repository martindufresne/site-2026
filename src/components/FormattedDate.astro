---
type Props = { date: Date };

const { date } = Astro.props;
const lang = Astro.currentLocale || 'fr'; // Detect current language
---

<time datetime={date.toISOString()}>
    {
        date.toLocaleDateString(lang === 'en' ? 'en-US' : 'fr-CA', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        })
    }
</time>